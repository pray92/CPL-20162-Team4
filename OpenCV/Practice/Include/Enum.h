#pragma once

typedef enum eScene
{
	SCENE_NONPASS,
	SCENE_MENU,
	SCENE_MOVIE,
	SCENE_END,
}SCENE;

enum TEXT_TYPE
{
	TEXTTYPE_SINGLE,
	TEXTTYPE_MULTI,
};

enum WINMODE
{
	WINMODE_FULL,
	WINMODE_WIN,
};

enum TYPE
{
	TYPE_TURTLE,
	TYPE_END,
};